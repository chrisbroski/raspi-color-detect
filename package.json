{
  "name": "raspi-color-detect",
  "version": "1.0.0",
  "description": "Color detection using Node.js and a Raspberry Pi camera",
  "main": "colorserver.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chris Broski",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^2.0.1"
  }
}
